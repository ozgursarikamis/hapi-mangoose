export enum Environment {
    Development = 'development',
    Test = 'test',
    Production = 'production'
}

export enum Host {
    IndexRoute = '/',
    Localhost = 'localhost',
    IPAddress = '0.0.0.0',
    DefaultPort = 4000
}